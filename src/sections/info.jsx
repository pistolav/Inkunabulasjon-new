import React from 'react';
import Carousel from "../elements/carousell/carousel";

const info = () => {
    return (
        <div className="info section">
            <Carousel/>
        </div>
    );
};

export default info;
